# Context 

This is an open source nextjs project, a ticketing website.

THe website will list events, and allow users to view event details.


## Getting the events data

You will be using our inhouse API client to connect to the cohost api.
The package is `@cohostvip/cohost-node`, it's already installed.

The config (URL , API key) is in the environment variables.
